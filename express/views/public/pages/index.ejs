<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Twitch Mod Squad</title>

<%- include("../partials/head") %>

</head>
<body>

<%- include("../partials/header") %>

    <main>
        <section class="heading landing">
            <div class="wrapper">
                <div class="heading-box">
                    <h1>
                        The Mod Squad
                    </h1>
                    <p>
                        Discord community supporting Twitch streamers and their communities.
                    </p>
                    <a href="https://patreon.com/twitchmodsquad" target="__blank" class="button full next secondary">
                        Support us on Patreon!
                    </a>
                    <a href="/join" class="button full next">
                        Join the Squad!
                    </a>
                </div>
                <div class="statistics blue">
                <% statistics.forEach((stat, i) => { %>
                    <div class="statistic<%= i === 0 ? " active" : "" %>" data-color="<%= stat.color %>">
                        <h2>
                            <%= stat.name %>
                        </h2>
                        <%= stat.value %>
                    </div>
                <% }) %>
                </div>
            </div>
        </section>
        <section class="solid">
            <div class="wrapper">
                <h2>
                    Featured Members
                </h2>
                <div class="user-container featured">
                <% featured.forEach(user => { %>
                <a href="https://twitch.tv/<%= user.login %>" target="__blank" class="user">
                    <img src="<%= user.profile_image_url %>" alt="Profile picture for <%= user.display_name %>">
                    <h3><%= user.display_name %><% if (user.broadcaster_type === "partner") { %> <img src="/assets/images/badges/twitch/partner.png" alt="Twitch Partner Badge" /><% } %></h3>
                    <% if (user.follower_count) { %><span class="followers"><%= comma(user.follower_count) %> follower<%= user.follower_count === 1 ? "" : "s" %></span><% } %>
                </a>
                <% }) %>
                </div>
                <div class="button-container">
                    <a href="/members" class="button">View All Participating Streamers</a>
                </div>
            </div>
        </section>
        <section>
            <div class="wrapper">
                <h2>Questions? Concerns?</h2>
                <p>Fill out our <a href="https://support.tms.to" target="__blank">support form</a>! Submissions can include your Discord username or can remain anonymous if you desire.</p>
                <p>Alternatively, you can reach out to <code>twijn</code> on Discord or <a href="/join">join The Mod Squad</a> to gain access to our channels.</p>
            </div>
        </section>
    </main>

<%- include("../partials/footer") %>


<%- include("../partials/javascript") %>
    <script src="/assets/js/public/statistics.js"></script>
</body>
</html>